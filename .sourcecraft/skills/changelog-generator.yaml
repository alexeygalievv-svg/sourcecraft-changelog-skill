name: changelog-generator
description: Генерирует CHANGELOG.md из git-истории ветки
inputs:
  - name: version
    type: string
    description: Номер релиза (например 1.2.0)
    required: true
  - name: branch
    type: string
    description: Ветка для git log
    default: main
  - name: lang
    type: choice
    description: Язык заголовков (ru или en)
    options:
      - ru
      - en
    default: ru
instructions: |
  Сгенерируй CHANGELOG.md для версии {{version}} из ветки {{branch}}.
  Запусти git log {{branch}} --no-merges.
  Сгруппируй: feat→✨, fix→🐛, refactor→♻️, chore→🔧, docs→📚.
  Язык: {{lang}}. Выведи только Markdown.
